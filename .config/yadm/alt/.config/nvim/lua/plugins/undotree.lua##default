return {
    "jiaoshijie/undotree",
    dependencies = {"nvim-lua/plenary.nvim"},
    config = function()
        require('undotree').setup({
            float_diff = true,
            layout = "left_bottom",
            position = "left",
            window = {winblend = 0}
        })
    end,

    keys = {{"<F5>", "<cmd>UndotreeToggle<cr>"}}
}
