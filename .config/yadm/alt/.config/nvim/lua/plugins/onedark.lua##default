return {
    "navarasu/onedark.nvim",
    enabled = true,
    config = function()
        require("onedark").setup({style = 'dark', transparent = true})

        vim.cmd("colorscheme onedark")
    end
}
